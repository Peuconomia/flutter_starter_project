part of 'home.dart';

class HomeTabletWidget extends StatelessWidget {
  const HomeTabletWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Tablet")),
    );
  }
}
